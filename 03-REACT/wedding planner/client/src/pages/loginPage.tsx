//import { useRouteError } from "react-router-dom";

export default function loginPage() {
  
  return (
    <div id="error-page">
      <h1>login!</h1>
      <p>Sorry, this page is still in working process.</p>
      <a href='/'>go back to main</a>
    </div>
  );
}